{
  "name": "avas-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/ --product hosted-service avastock/avas-subgraph",
    "create-local": "graph create --node http://localhost:8020/ avastock/avas-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ avastock/avas-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 avastock/avas-subgraph"
  },
  "dependencies": {
     "@graphprotocol/graph-cli": "0.20.0",
     "@graphprotocol/graph-ts": "0.20.0"
  }
}
